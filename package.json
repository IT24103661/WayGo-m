{
  "name": "waygo",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix backend install",
    "start": "npm --prefix backend start",
    "build": "npm --prefix frontend install && npm --prefix frontend run build && rm -rf build && cp -R frontend/build ./build"
  },
  "dependencies": {
    "react-icons": "^5.6.0"
  }
}
